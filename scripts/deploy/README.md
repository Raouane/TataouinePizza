# Scripts Déploiement V2

Scripts organisés pour le déploiement et la synchronisation.

## Structure

```
scripts/deploy/
├── render.ts          # Déploiement Render (à créer)
└── sync-prod.ts       # Synchronisation prod (à créer)
```

## Migration depuis l'ancien système

Les scripts existants dans `script/` continuent de fonctionner.
Les nouveaux scripts V2 seront créés progressivement.

## Scripts existants (à migrer)

- `script/sync-to-production.ts` → `scripts/deploy/sync-prod.ts`
